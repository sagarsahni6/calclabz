{
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "verify": "node scripts/verify-calculators.js && node scripts/verify-seo.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
